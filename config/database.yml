# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: tokopedia_heqi_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

  username: studio
  password: aaa
  host: localhost
  port: 5432

test:
  <<: *default
  database: tokopedia_heqi_test

production:
  <<: *default
  database: tokopedia_heqi_production
  username: proj
  password: <%= ENV['PROJ_DATABASE_PASSWORD'] %>